# Imports the app directory, running its __init__ file.
# This creates the app and supports a neat project structure
# This must be the target of your run/debug configuration in order to work
from app import app